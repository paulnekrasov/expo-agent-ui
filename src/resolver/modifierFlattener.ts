import type { ViewNode } from "../ir/types";

export function flattenModifiers(nodes: ViewNode[]): ViewNode[] {
  // STUB: Stage 3 scaffold only. Preserve source-order modifiers unchanged for now.
  return nodes;
}
