import type { ViewNode } from "../ir/types";

export function applyStateStubs(nodes: ViewNode[]): ViewNode[] {
  // STUB: Stage 3 scaffold only. Property-wrapper stub injection lands later.
  return nodes;
}
