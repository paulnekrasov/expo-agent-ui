{
  "name": "@expo-agent-ui/mcp-server",
  "version": "0.0.0",
  "description": "Local MCP server package shell for Expo Agent UI.",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "agent-ui-mcp": "dist/cli.js"
  },
  "files": [
    "dist",
    "dist/skills",
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "tsc -p tsconfig.json && node scripts/copy-skills.js",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "@expo-agent-ui/core": "*",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/ws": "^8.5.0",
    "ts-jest": "^29.1.0"
  }
}
