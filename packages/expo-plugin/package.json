{
  "name": "@expo-agent-ui/expo-plugin",
  "version": "0.1.0",
  "description": "Optional Expo config plugin shell for Expo Agent UI.",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "app.plugin.js",
    "dist",
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "tsc -p tsconfig.json",
    "test": "node -e \"console.log('@expo-agent-ui/expo-plugin tests deferred until native mutations exist')\" --"
  },
  "peerDependencies": {
    "expo": "^55.0.0"
  }
}
