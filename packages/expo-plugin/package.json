{
  "name": "@expo-agent-ui/expo-plugin",
  "version": "1.0.0",
  "description": "Optional Expo config plugin shell for Expo Agent UI.",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "app.plugin.js",
    "dist",
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "tsc -p tsconfig.json",
    "test": "node -e \"require('child_process').execSync('tsc -p tsconfig.json',{stdio:'inherit'}); require('./test/plugin.test.cjs')\" --"
  },
  "peerDependencies": {
    "expo": "^55.0.0"
  }
}
