{
  "name": "@expo-agent-ui/cli",
  "version": "0.0.0",
  "description": "Command-line package shell for Expo Agent UI.",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "agent-ui": "dist/cli.js"
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "tsc -p tsconfig.json",
    "test": "jest --config jest.config.js --runInBand"
  },
  "dependencies": {
    "@expo-agent-ui/mcp-server": "0.0.0"
  }
}
